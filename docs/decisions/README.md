# Architecture Decision Records

Store project-level architecture decisions here when the self-healing workflows introduce non-obvious tradeoffs.

Suggested topics:

- Why workflow static data is used instead of filesystem writes inside n8n Code nodes
- How retry, fallback, and escalation are classified
- Why the simulator exists as a separate workflow package
